using System;
// added a rank on this program
class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();
        goalManager.Start();
        //create a goalmanager object
        // call the start function on that object

    }
}